export interface viewType {
    id: string;
    name: string;
    icon: string;
    path: string;
}

export interface NavItemPropsType {
    id: string;
    name: string;
    icon: string;
    path: string;
    active: boolean;
}